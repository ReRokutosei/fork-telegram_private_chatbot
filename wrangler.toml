name = 'terminal-aide-0017'
main = "worker.js"
compatibility_date = "2024-11-30"

# 注意：KV 命名空间 ID 每个用户都不一样，
# 所以不能写死在这里。用户需要在 Cloudflare 后台手动绑定。

# Durable Object 绑定配置
# 用户需要：
# 1. 在 Cloudflare 后台创建 Durable Object 绑定，指向 RateLimitDO
# 2. 设置环境变量 RATE_LIMIT_DO 指向 DO 实例
[env.production]
# 示例配置（需要用户手动设置）
# [[durable_objects.bindings]]
# name = "RATE_LIMIT_DO"
# class_name = "RateLimitDO"
# script_name = "terminal-aide-0017"

